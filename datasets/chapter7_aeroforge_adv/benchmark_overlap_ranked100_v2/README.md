# Repair benchmark — overlap_ranked100_v2

Thesis anchor: `tab:aeroforge-overlap-repair`, `tab:aeroforge-overlap-repair-grid`,
`tab:aeroforge-aero-preserve` (all use the same 100-start benchmark).

**File:** `overlap_ranked100_v2.json` (173 KB)

100 worst-overlap starts from the 100k labeled pool, ranked by overlap volume.
Each entry embeds the full 43-key ADV plus rank metadata. Optimizer seed: **12345**;
CAD verification threshold τ_decide = **1 mm³**.
